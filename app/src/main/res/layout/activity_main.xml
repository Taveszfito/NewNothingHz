<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="?android:attr/colorBackground"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Spacer a tetejére, hogy az egész UI lejjebb kerüljön -->
    <View
        android:layout_width="match_parent"
        android:layout_height="80dp" />

    <!-- App címsor -->
    <com.google.android.material.textview.MaterialTextView
        android:text="Nothing 120Hz Unlock"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="?android:attr/textColorPrimary"
        android:fontFamily="@font/nothingdot"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <!-- Állapot kártya -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp">

        <LinearLayout
            android:orientation="vertical"
            android:padding="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/statusText"
                android:text="Overlay: OFF"
                android:textSize="16sp"
                android:textColor="?android:attr/textColorSecondary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <com.google.android.material.switchmaterial.SwitchMaterial
                android:id="@+id/switchOverlay"
                android:text="Enable floating overlay"
                android:layout_marginTop="8dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Engedélyek szekció -->
    <com.google.android.material.textview.MaterialTextView
        android:text="Permissions"
        android:textStyle="bold"
        android:textSize="18sp"
        android:layout_marginTop="24dp"
        android:layout_width="wrap_content"
        android:fontFamily="@font/nothingdot"
        android:layout_height="wrap_content" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCheckOverlay"
        android:text="Check overlay permission"
        android:layout_marginTop="8dp"
        style="@style/Widget.Material3.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnRequestOverlay"
        android:text="Request overlay permission"
        android:layout_marginTop="8dp"
        style="@style/Widget.Material3.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnBatteryOpt"
        android:text="Battery optimization exception (optional)"
        android:layout_marginTop="8dp"
        style="@style/Widget.Material3.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/switchAutostart"
        android:text="Start overlay on boot (optional)"
        android:layout_marginTop="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- About blokk -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="2dp">

        <LinearLayout
            android:orientation="vertical"
            android:padding="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.google.android.material.textview.MaterialTextView
                android:text="About this app"
                android:textStyle="bold"
                android:textSize="18sp"
                android:layout_width="wrap_content"
                android:fontFamily="@font/nothingdot"
                android:layout_height="wrap_content" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/aboutText"
                android:text="This app places an invisible 1-pixel overlay at the edge of the screen to help your device maintain 120Hz in games and apps.

100% offline: it never connects to the internet.

No data collection: nothing is stored or shared from your device.

Lightweight and minimal: runs only when you enable it.

It has no effect on adaptive refresh. This means your phone can still achieve the lowest possible refresh rate when 120Hz is not needed.

💡 Pro tip: Add a Quick Settings tile for instant ON/OFF without opening the app."
                android:textSize="14sp"
                android:textColor="?android:attr/textColorSecondary"
                android:layout_marginTop="8dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Spacer, hogy a footer lemenjen az aljára -->
    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Footer -->
    <TextView
        android:id="@+id/footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Developed by Due-Boysenberry-1226 (Reddit), with love ❤"
        android:textSize="12sp"
        android:textColor="@android:color/darker_gray"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp" />

</LinearLayout>
